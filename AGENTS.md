# AGENTS.md — 项目 AI 编码强制约束规范（最终完整版）
> 适用于：Cursor / GitHub Codex / 所有AI编码
> 生效范围：本仓库全部AI生成、修改、重构、SQL编写行为
> 优先级：本文件规则 **高于AI默认行为、高于通用编码习惯**，必须严格强制执行

## 0. 核心总则（最高优先级，所有任务优先遵守）
1. **最小改动原则**：仅解决当前需求，禁止主动重构、禁止无关代码优化、禁止统一格式、禁止清理冗余、禁止修改无关文件。
2. **禁止自作主张**：需求未提及的功能、字段、校验、逻辑、索引、约束，一律不新增、不修改、不删除。
3. **不确定即询问**：业务模糊、表结构不明、原有逻辑复杂、存在多实现场景 → 先提问，**禁止猜测、禁止盲写、禁止补全逻辑**。
4. **风格跟随原则**：完全沿用项目现有编码风格、命名、注释、工具类、写法，不引入个人/新框架风格。
5. **单任务单语句原则**：所有DDL、DML、代码修改，**一条语句只做一件事**，禁止合并操作。

## 1. Java 后端强制规范
### 1.1 枚举 & Codes 强约束（核心红线）
1. 所有 `code / status / type / category / flag` 编码字段，**必须使用项目枚举 + @EnumValue** 映射数据库。
2. **全程禁止魔法数字、魔法字符串**，所有状态判断、赋值只能引用枚举常量。
3. 不允许业务代码出现 `==0`、`==1`、`status="0"` 等硬编码。
4. 新增业务编码值时：**先新增枚举项，再写业务逻辑**，绝不裸写编码数字。
5. 接口入参、业务判断、数据库入库全程枚举闭环，杜绝非法编码。

### 1.2 MyBatis & MyBatisPlus 规范
1. 普通CRUD **优先使用MyBatisPlus自带方法**，禁止手写简单SQL。
2. 复杂多表、分组、统计查询必须写XML，**禁止Java代码内拼接SQL**。
3. 禁止擅自新增/修改/删除表、字段、索引、约束，表结构变更必须先输出DDL确认。
4. 禁止随意加DISTINCT、ORDER BY、全表查询，必须精准按需查询。

### 1.3 分层架构强制约束
1. Controller：只做**参数接收、参数校验、调用Service、统一结果返回**，禁止写任何业务逻辑。
2. Service：承载所有核心业务逻辑、事务控制、业务校验、幂等处理。
3. 入参必须标配JSR303校验：`@NotBlank`/`@NotNull`/`@Size`/`@Pattern`，编码类参数做枚举校验。
4. **登录用户获取强制约束（新增）**
   - 获取当前登录用户，**只允许在 Controller 层执行**，统一使用 `CurrLoginUser` 获取；Service、Mapper、工具类内部**禁止直接获取登录用户**。
   - Service 需要登录用户信息时，由 Controller 将用户ID/用户对象作为**方法入参传入**，Service 内部不从上下文、工具类直接拿登录用户。
   - 历史已有代码不主动修改、不重构；仅对AI**新生成/新增的代码片段强制生效**。
   - ❌ 禁止：在Service、工具类中调用获取当前登录用户的工具/上下文。
   - ✅ 正确：Controller 获取 CurrLoginUser → 把 userId / loginUser 传给 service 方法参数。

### 1.4 接口查询参数属性约束
1. **精确匹配查询**：直接使用数据库实体原字段名作为入参属性，直接等值匹配，不拼接通配符。
2. **模糊查询参数命名规则**：模糊查询入参属性以 `Like` 结尾，例如：`nameLike`、`phoneLike`、`caseNameLike`。
3. **模糊查询SQL规则**
    - 属性名以`Like`结尾代表普通模糊查询，对应 MyBatis‑Plus `like()`，等价于 `column LIKE '%xxx%'`，左右都带百分号。
    - 后缀匹配（仅右侧%）使用 `xxxLikeRight` 命名，对应 MyBatis‑Plus `likeRight()`，等价于 `column LIKE 'xxx%'`。
4. 禁止：同一个字段既做精确查询又复用同一个入参属性；精确、模糊、后缀匹配必须分开不同入参字段。
    - 示例：
        - 精确查询：`name`（等值匹配）
        - 全模糊查询：`nameLike`（%xxx%）
        - 后缀匹配：`nameLikeRight`（xxx%）
5. 参数校验：`xxxLike` / `xxxLikeRight` 模糊类参数不允许加 `@NotBlank`，允许为空；为空时直接忽略该查询条件。
6. 禁止在Controller层手动拼接百分号；通配符拼接逻辑统一交给MyBatis‑Plus条件方法，放在Service层。

> 示例MP写法：
> ```java
> // 全模糊 %xxx%
> wrapper.like(StringUtils.isNotBlank(query.getNameLike()), Entity::getName, query.getNameLike());
> // 后缀模糊 xxx%
> wrapper.likeRight(StringUtils.isNotBlank(query.getNameLikeRight()), Entity::getName, query.getNameLikeRight());
> ```

## 2. DDL / SQL 终极强制规范
### 2.1 建表语句规则
1. **CREATE TABLE 仅允许存在主键约束**。
2. 建表语句内 **严禁包含**：唯一约束、普通索引、联合索引、CHECK约束、外键约束。
3. 所有索引、约束**必须在建表完成后，单独单条SQL创建**。
4. 主键统一规范：
    - 主键字段名 **固定为 id，无任何业务前缀**
    - 无特殊说明时，主键类型统一为 `BIGINT AUTO_INCREMENT`
    - 禁止：case_id、order_id、record_id 等自定义主键名

### 2.2 ALTER TABLE 改表规则（严格单条单操作）
1. **一条ALTER SQL 只允许执行一个操作**：
    - 一条SQL仅新增一个字段
    - 一条SQL仅修改一个字段
    - 一条SQL仅删除一个字段
    - 一条SQL仅创建一个索引/一个约束
2. **严禁合并操作**：不允许单语句新增多字段、创建多索引。
3. **严禁调整列位置**：禁止使用 `FIRST` / `AFTER 字段` 语法，绝不改动原有列顺序。
4. 所有索引、唯一约束、CHECK约束、外键，**全部一约束一语句**。

### 2.3 DML 数据操作规则
1. INSERT：**单条SQL仅插入单行数据**，禁止批量多行INSERT语法。
2. UPDATE：**单条SQL仅更新单条数据**，必须基于主键ID更新，禁止批量全表/无条件更新。
3. DELETE：禁止无主键、无精准条件的删除语句。

### 2.4 SQL 禁止清单（绝对红线）
❌ 禁止建表语句内定义任何索引、唯一、外键、CHECK约束
❌ 禁止单ALTER语句批量新增字段、批量创建索引
❌ 禁止使用AFTER/FIRST调整列顺序
❌ 禁止自定义主键字段名、非特殊情况不用bigint自增主键
❌ 禁止单SQL多行INSERT、批量UPDATE
❌ 禁止数据库层硬编码状态值，业务约束全部走枚举+代码层校验

## 3. 代码禁止行为清单（绝对不允许）
1. ❌ 禁止自动重构、优化、格式化、整理历史代码
2. ❌ 禁止修改权限、登录、Token、Security、认证、全局拦截器核心代码
3. ❌ 禁止修改项目工具类、常量类、统一返回体、全局配置
4. ❌ 禁止升级/修改pom、yml无关依赖与配置
5. ❌ 禁止删除原有逻辑、注释、业务分支
6. ❌ 禁止超需求开发、新增未要求的功能
7. ❌ 禁止在Service / Mapper / 工具类内部直接获取当前登录用户（新代码强制约束，历史代码不动）

## 4. AI 固定输出模板（每次完成必须输出）
每一次编码/修改/SQL编写完成，**必须完整输出以下四项**：
1. 【改动文件清单】所有新增、修改、变动的文件路径
2. 【核心Diff变更】关键代码/SQL改动片段
3. 【自测验证步骤】可直接复现、可直接测试的步骤
4. 【风险说明】改动兼容点、影响范围、潜在风险

## 5. 交付完成标准
1. 完全满足当前需求，无超范围改动
2. 无魔法数字、无违规SQL、无格式乱改
3. 所有SQL严格遵守：单语句单操作、建表无额外约束、无列位置调整
4. 查询参数严格遵守命名约定：`xxxLike`对应全模糊`like()`；`xxxLikeRight`对应后缀模糊`likeRight()`；精确查询使用原生字段名
5. **登录用户获取规则：新代码仅Controller层使用CurrLoginUser获取，用户信息通过入参传递到Service，不改动历史存量代码**
6. 编译无错、原有业务不崩、新旧逻辑兼容
7. 完全贴合项目原有架构与编码风格

## 6. 兜底最高规则
本文件所有规则为**项目最高强制规范**，但凡与AI默认行为、通用开发规范冲突，**一律以本AGENTS.md为准**。
任何不确定、有风险、模糊的操作，必须先询问，禁止自动执行。
