<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.medcase.system.mapper.SysMenuMapper">

	<resultMap type="com.medcase.system.entity.SysMenuEntity" id="SysMenuResult">
		<id     property="id"             column="id"             />
		<result property="menuName"       column="menu_name"      />
		<result property="parentId"       column="parent_id"      />
		<result property="orderNum"       column="order_num"      />
		<result property="routePath"      column="route_path"     />
		<result property="vueComponentPath" column="vue_component_path" />
		<result property="routeName"      column="route_name"     />
		<result property="menuType"       column="menu_type"      />
		<result property="visible"        column="visible"        />
		<result property="status"         column="status"         />
		<result property="perms"          column="perms"          />
		<result property="icon"           column="icon"           />
		<result property="createBy"       column="create_by"      />
		<result property="createTime"     column="create_time"    />
		<result property="updateTime"     column="update_time"    />
		<result property="updateBy"       column="update_by"      />
		<result property="remark"         column="remark"         />
	</resultMap>

	<sql id="selectMenuVo">
        select id, menu_name, parent_id, order_num, route_path, vue_component_path, route_name, menu_type, visible, status, ifnull(perms,'') as perms, icon, create_time
		from sys_menu
    </sql>

    <select id="selectMenuList" resultMap="SysMenuResult">
		<include refid="selectMenuVo"/>
		<where>
			<if test="menu.menuName != null and menu.menuName != ''">
				AND menu_name like concat('%', #{menu.menuName}, '%')
			</if>
			<if test="menu.visible != null">
				AND visible = #{menu.visible}
			</if>
			<if test="menu.status != null and menu.status != ''">
				AND status = #{menu.status}
			</if>
		</where>
		order by parent_id, order_num
	</select>

	<select id="selectMenuTreeAll" resultMap="SysMenuResult">
		select distinct m.id, m.parent_id, m.menu_name, m.route_path, m.vue_component_path, m.route_name, m.visible, m.status, ifnull(m.perms,'') as perms, m.menu_type, m.icon, m.order_num, m.create_time
		from sys_menu m where m.menu_type in ('M', 'C') and m.status = 0
		order by m.parent_id, m.order_num
	</select>

	<select id="selectMenuListByUserId" resultMap="SysMenuResult">
		select distinct m.id, m.parent_id, m.menu_name, m.route_path, m.vue_component_path, m.route_name, m.visible, m.status, ifnull(m.perms,'') as perms, m.menu_type, m.icon, m.order_num, m.create_time
		from sys_menu m
		left join sys_role_menu rm on m.id = rm.menu_id
		left join sys_user_role ur on rm.role_id = ur.role_id
		left join sys_role ro on ur.role_id = ro.role_id
			where ur.user_id = #{userId}
			<if test="menu.menuName != null and menu.menuName != ''">
	            AND m.menu_name like concat('%', #{menu.menuName}, '%')
			</if>
			<if test="menu.visible != null">
	            AND m.visible = #{menu.visible}
			</if>
			<if test="menu.status != null and menu.status != ''">
	            AND m.status = #{menu.status}
			</if>
		order by m.parent_id, m.order_num
	</select>

    <select id="selectMenuTreeByUserId" parameterType="java.lang.Long" resultMap="SysMenuResult">
		select distinct m.id, m.parent_id, m.menu_name, m.route_path, m.vue_component_path, m.route_name, m.visible, m.status, ifnull(m.perms,'') as perms, m.menu_type, m.icon, m.order_num, m.create_time
		from sys_menu m
			 left join sys_role_menu rm on m.id = rm.menu_id
			 left join sys_user_role ur on rm.role_id = ur.role_id
			 left join sys_role ro on ur.role_id = ro.role_id
			 left join sys_user u on ur.user_id = u.user_id
		where u.user_id = #{userId} and m.menu_type in ('M', 'C') and m.status = 0  AND ro.status = 0
		order by m.parent_id, m.order_num
	</select>

	<select id="selectMenuPerms" resultType="java.lang.String">
		select distinct m.perms
		from sys_menu m
			 left join sys_role_menu rm on m.id = rm.menu_id
			 left join sys_user_role ur on rm.role_id = ur.role_id
	</select>

	<select id="selectMenuPermsByUserId" parameterType="java.lang.Long" resultType="java.lang.String">
		select distinct m.perms
		from sys_menu m
			 left join sys_role_menu rm on m.id = rm.menu_id
			 left join sys_user_role ur on rm.role_id = ur.role_id
			 left join sys_role r on r.role_id = ur.role_id
		where m.status = '0' and r.status = '0' and ur.user_id = #{userId}
	</select>

	<select id="selectMenuPermsByRoleId" parameterType="java.lang.Long" resultType="java.lang.String">
		select distinct m.perms
		from sys_menu m
			 left join sys_role_menu rm on m.id = rm.menu_id
		where m.status = '0' and rm.role_id = #{roleId}
	</select>

	<select id="selectMenusByPathOrRouteName" resultMap="SysMenuResult">
		<include refid="selectMenuVo"/>
		where menu_type in ('M', 'C') and (route_path = #{routePath} or route_name = #{routeName})
	</select>

</mapper>
